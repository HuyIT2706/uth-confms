# //env => conference-service 
# NODE_ENV=development
# PORT=3002

# # Database Config
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=admin
# DB_PASSWORD=admin123
# DB_DATABASE=db_conference

# ===================================================================
# Conference Service - UTH-ConfMS (.env)
# ===================================================================

NODE_ENV=development
PORT=3002

# -------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# -------------------------------------------------------------------
DB_HOST=postgres         # ← ĐÃ SỬA: Không dùng localhost nữa, dùng tên service trong Docker
DB_PORT=5432
DB_USERNAME=admin
DB_PASSWORD=admin123
DB_DATABASE=db_conference

# -------------------------------------------------------------------
# SMTP Configuration (Gửi email thông báo)
# -------------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com               # Thay bằng email Gmail thật của bạn
SMTP_PASS=your-app-password                   # App Password từ Google (không dùng mật khẩu thường)
SMTP_FROM=no-reply@uth-confms.vn              # Email hiển thị khi gửi đi

# -------------------------------------------------------------------
# OpenAI API (Tích hợp AI tùy chọn: gợi ý từ khóa, soạn email, tóm tắt)
# -------------------------------------------------------------------
OPENAI_API_KEY=sk-your-openai-key-here       # Thay bằng key thật của bạn (bắt buộc nếu dùng AI)

# -------------------------------------------------------------------
# Microservices URLs (Tích hợp với các service khác)
# -------------------------------------------------------------------
IDENTITY_SERVICE_URL=http://identity-service:3001/api   # Để lấy email, thông tin user
REVIEW_SERVICE_URL=http://review-service:3004/api       # Để lấy điểm đánh giá tổng hợp

# -------------------------------------------------------------------
# JWT Configuration (Phải trùng khớp 100% với identity-service)
# -------------------------------------------------------------------
JWT_ACCESS_SECRET=jZE6YIUoP_j7SOTLPWgS8kSfX5g4dlOmPMWJVNLMOyg-SMoqXiMRkR0ocJQEGr9HVUjonNIlZNwHzduFfOCJOQ

# ===================================================================
# Hướng dẫn sử dụng
# ===================================================================
# 1. Copy file này vào thư mục gốc của conference-service
# 2. Thay các giá trị sau bằng thông tin thật của bạn:
#    - SMTP_USER & SMTP_PASS
#    - OPENAI_API_KEY (nếu muốn dùng AI)
# 3. Khi chạy với Docker Compose, các service sẽ tự resolve tên:
#    identity-service:3001 và review-service:3004
# 4. Nếu chạy local (npm run start:dev), có thể thay bằng localhost:
#    IDENTITY_SERVICE_URL=http://localhost:3001/api
#    REVIEW_SERVICE_URL=http://localhost:3004/api