// DEPRECATED: This file was replaced by `routes.config.tsx` which contains the
// canonical, typed route definitions (with JSX allowed). Keep this stub to
// avoid editor tooling/paths resolving issues during an incremental migrate.

// If you see this file, prefer importing from './routes.config.tsx' instead.

export {}
